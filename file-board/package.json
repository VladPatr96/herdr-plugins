{
  "name": "herdr-file-board",
  "version": "0.1.0",
  "private": true,
  "description": "A file manager in a Herdr pane: the tree next to your agent, a look inside a file, and nvim when you mean to change it",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test"
  }
}
