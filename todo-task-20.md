# Задача #20: что сделать вам

Задача: https://github.com/VladPatr96/herdr-plugins/issues/20

Плагин `agent-tasks` собран и проверён живыми запусками.

**22 сентября вы попросили дописать конфиг — сделано.** Что изменилось вне
репозитория, перечислено ниже; откат — в конце файла.

## 1. Строка задачи в сайдбаре — добавлена

herdr показывает токены плагина только там, где вы их попросите. В
`%APPDATA%\herdr\config.toml` у агента было две строки; теперь три — третья и
есть задача:

```toml
[ui.sidebar.agents]
rows = [
  ["state_icon", "machine", "workspace", "tab"],
  ["agent", { token = "$hotkey", align = "right", fg = "#f9e2af", bold = true }],
  [
    { token = "$task", bold = true },
    { token = "$agents", fg = "#f38ba8", bold = true },
    { token = "$task_progress", align = "right", fg = "#a6e3a1" },
  ],
]
```

Одна строка служит обоим: у агента заполнен `$task`, у сессии, которая его
запустила, — `$agents` (`▶ 3 агента`, розовым). У всех прочих не заполнено
ничего, и строка пропадает.

> `align` понимает только ваша сборка herdr (`VladPatr96/herdr@sidebar-token-align`).
> Стоковый herdr её отвергает — там уберите `align` из обеих строк.

Конфиг перечитан (`herdr server reload-config` — `status: applied`, без
замечаний), так что строка уже работает.

Заодно снят отбор панели «Agents»: он стоял на «прятать агентов задач», и с ним
никакая строка задачи в сайдбаре не показалась бы. Вернуть —
`herdr plugin action invoke hide-from-sidebar --plugin vladpatr96.agent-tasks`.

Строка **пропадает у агентов, запущенных не этим плагином**: herdr прячет
строку, у которой ни один токен не заполнен. Остальные агенты выглядят как
раньше.

Заголовок задачи, впрочем, виден у вас уже сейчас: плагин кладёт его и в
название вкладки, а `tab` стоит в первой строке. Третья строка добавляет к
нему прогресс плана (`4/4`) и текущий пункт.

## 1a. Хоткеи на список агентов — добавлены

Раньше окно со списком открывалось только из командной строки. Теперь в
`config.toml` есть две привязки — по образцу `quota-board` и `file-board`:

```toml
[[keys.command]]
key = "alt+shift+a"
type = "plugin_action"
command = "vladpatr96.agent-tasks.open-side"
description = "Agents and tasks (side pane)"

[[keys.command]]
key = "alt+shift+s"
type = "plugin_action"
command = "vladpatr96.agent-tasks.open"
description = "Agents and tasks (overlay)"
```

- **Alt+Shift+A** — список сбоку от панели, в которой нажали. Остаётся
  открытым, агенты сменяются под ним.
- **Alt+Shift+S** — тот же список оверлеем: наезжает на активную панель и по
  `q` возвращает фокус обратно.

Плата обычная: эти две клавиши теперь перехватывает herdr и до TUI внутри
панелей они не доходят — как `Alt+Shift+Q` у квот и `Alt+Shift+F` у файлов.

## 2. Посмотреть, что получилось

Сейчас запущены пять агентов, все уже отработали: три claude
(`Alt+8`, `Alt+9`, `Alt+Q`) и два agy (`Alt+0` и `Alt+W` — проверка запуска на
другой модели, `gemini-3.1-pro-high`).

- [ ] Под именем агента видна его задача, справа `4/4`.
- [ ] **У вашей собственной сессии** (той, из которой запускали) в этой же
      строке стоит `▶ N агентов` — по ней оркестратор отличается от агентов.
      Закроете всех — пометка уйдёт.
- [ ] `Alt+8` открывает его сессию: видна переписка, можно спросить и получить
      ответ. Это обычная сессия, не субагент.
- [ ] Действие `Agent Tasks: open the board` (или уже открытый пейн «Агенты и
      задачи») показывает всех агентов с задачами, статусами и прогрессом;
      `p` разворачивает чек-листы, `q` закрывает.
- [ ] **Агент показывается под списком:** `↑` `↓` выбирают, `Enter`
      (или `1`…`9`) подставляет панель агента под список, а прежнюю
      увозит обратно в её вкладку. `o` отпускает агента, `q` закрывает список
      и тоже отправляет агента домой.
- [ ] Уехавший агент остался собой: тот же хоткей, та же строка в сайдбаре,
      вкладка снова называется его задачей.

## 3. Запустить агента самому

Скилл переустановлен глобально — `~/.claude/skills/zapustit-agenta/SKILL.md`,
с настоящим путём плагина внутри. Он виден в **любой** сессии Claude Code, в
любом проекте:

```
/zapustit-agenta посмотри, что в папке quota-board, и расскажи одним абзацем, моделью sonnet
```

- [ ] В сайдбаре появился новый агент со своей задачей.
- [ ] Через полминуты у него в строке пошёл прогресс плана (`1/4`, `2/4`…).
- [ ] Вошли в него по хоткею, задали вопрос — ответил.

## 4. Сообщить результат

- **Всё работает:** отправлю задачу на приёмку, её проверит агент с чистым
  контекстом.
- **Что-то не так:** опишите, что видите (лучше со скриншотом), поправлю.

## Что известно про codex

Codex запускается тем же способом и **попадает в общий список**: своя вкладка,
своя задача в сайдбаре, свой хоткей, статус, строка на доске — это проверено.
Но **отвечать он на этой машине сейчас не может**, и плагин тут ни при чём:

- `codex` в этой папке, запущенный вручную (без herdr), падает с
  `windows sandbox failed: timed out after 15000ms connecting runner pipe-in`;
- в другой папке он доходит до композитора, но остаётся в `model: loading` и
  копит ввод в «Queued follow-up inputs», не начиная сессию;
- `codex doctor` сам не завершается.

Квота codex при этом целая (7d — 24%).

`codex doctor` называет причину прямо: **`app-server background server socket
is stale or unreachable`**, и `codex app-server daemon version` подтверждает —
`failed to connect to C:\Users\user\.codex\app-server-control\app-server-control.sock`
(«конечный компьютер отверг запрос на подключение», os error 10061). В папке
`~/.codex/app-server-daemon/` нет ни `settings.json`, ни pid-файлов. Сессия
codex поднимается через этот демон, поэтому TUI и застревает на
`model: loading`.

Чинится это на стороне codex, не плагина: поднять демон заново
(`codex app-server daemon` или `codex update`). Вашу установку codex я не
трогал — это за пределами задачи.

Плагин в этом случае честно отвечает `queued: true` и **второй раз задачу не
шлёт**, чтобы она не пришла дважды.

Если почините codex — проверьте пункт 3 с `--kind codex`, и тогда условие
готовности закроется целиком.

## Убрать агентов из сайдбара

Когда агент виден под списком, вторая его строка в панели «Agents» только
мешает. Убирается действием:

```
herdr plugin action invoke hide-from-sidebar --plugin vladpatr96.agent-tasks
```

Вернуть — `show-in-sidebar`. Сейчас пряталка **выключена**: агенты задач
видны в сайдбаре вместе со своей строкой задачи.

Прячет не плагин, а сам herdr: у него есть отбор этой панели
(`agent.view.set`), и отбирает он по токену `$task`, который ставит только
этот плагин. Чужие агенты остаются на месте, `agent list` и хоткеи
продолжают видеть всех — прячется только строка.

- [ ] Агентов задач в панели «Agents» не видно, остальные агенты на месте.
- [ ] `show-in-sidebar` возвращает их обратно.

## Отдельно: режим разрешений

Файл задачи лежит в папке состояния плагина, а не в репозитории. Читать её
claude разрешено — запуск передаёт ему `--add-dir` с этой папкой. А вот
**править** файлы без подтверждения решает ваш режим разрешений, и в `manual
mode` агент спросит разрешения дописать план в свой же файл задачи.

Плагин режим за вас не меняет — это ваш выбор. Не хотите подтверждать:

```
node .../bin/launch.js … --permission-mode acceptEdits
```

Скилл сам этот флаг не добавляет; скажите ему, если нужно.

## Отдельно: ваш глобальный хук

В каждой новой сессии Claude Code — и в запущенных агентах тоже — в переписке
мелькает:

```
⎿  UserPromptSubmit hook error
⎿  Failed with non-blocking status code: node:internal/modules/cjs/loader:1386
```

Это `Cannot find module`: какой-то хук в вашем `~/.claude/settings.json`
(похоже, сторож `zadacha/hooks/guard.mjs`) указывает на несуществующий путь.
К плагину отношения не имеет и ничего не ломает — хук объявлен non-blocking, —
но видно его будет у каждого запущенного агента.

## Если что-то пошло не так: откат

Конфиг целиком, каким он был до правки, лежит рядом:

```
copy "%APPDATA%\herdr\config.toml.bak-agent-tasks" "%APPDATA%\herdr\config.toml"
herdr server reload-config
```

Это вернёт и две строки сайдбара вместо трёх, и уберёт `Alt+Shift+A` /
`Alt+Shift+S`. Остальное отключается по отдельности:

```
node D:/projects/my_projects/herdr_plugins/agent-tasks/bin/install-skill.js --uninstall
herdr plugin unlink vladpatr96.agent-tasks
```

Запущенные агенты при этом остаются на месте — это обычные вкладки herdr.

## 5. После приёмки

- [ ] Откройте PR из ветки `task/20-orkestrator-agentov-v-herdr-sobrat` в `main`
      и смёржите его:
      https://github.com/VladPatr96/herdr-plugins/pull/new/task/20-orkestrator-agentov-v-herdr-sobrat
- [ ] Скажите «принято», и я закрою задачу и запишу разбор в hq.
