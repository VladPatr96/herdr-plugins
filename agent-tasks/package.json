{
  "name": "herdr-agent-tasks",
  "version": "0.1.0",
  "private": true,
  "description": "Give a task to a whole agent session in Herdr, with its task, status and plan in the sidebar",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test"
  }
}
